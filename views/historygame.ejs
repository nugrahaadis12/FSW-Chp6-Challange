<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="../css/style.css">
    <title>Document</title>
</head>

<body>
    <h1 class="text-center">History on Game <%= data.nameGame %>
    </h1>
    <div class="input-form text-center">
        <div class="form-group">
            <textarea class="form-control" placeholder="Input update games" id="update-input" rows="3"></textarea>
        </div>
        <div class="form-group">
            <input type="text" class="form-control" placeholder="Input version" id="version-input">
        </div>
        <div class="form-group">
            <input type="number" class="form-control" placeholder="Input Rating" id="rating-input">
        </div>
        <button type="submit" class="btn btn-primary" onclick=handleInputHistoryGame(<%=data.id %>)>Insert</button>
        <button type="submit" class="btn btn-warning" id="btnEdit" disabled>Submit Edit</button>
    </div>

    <div class="table-data">
        <table class="table">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Update</th>
                    <th scope="col">Version</th>
                    <th scope="col">Rating</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                <% for( let index=0; index < data.historyGames.length; index++ ) { %>
                    <tr>
                        <th scope="row">
                            <%= index + 1%>
                        </th>
                        <td>
                            <%= data.historyGames[index].update %>
                        </td>
                        <td>
                            <%= data.historyGames[index].version %>
                        </td>
                        <td>
                            <%= data.historyGames[index].rating %>
                        </td>
                        <td>
                            <button type="submit" class="btn btn-primary" onclick=handleEditHistory()>Edit</button>
                            <button type="submit" class="btn btn-primary"
                                onclick=handleDeleteHistory(<%=data.historyGames[index].id%>)>Delete</button>
                        </td>
                    </tr>
                    <% } %>
            </tbody>
        </table>
    </div>
    <div>
        <h3 class="text-center"><a href=<%=`/dashboard_game/` %>>< Back to List Game</a></button></h3>
    </div>
    <script src="../js/script.js"></script>
</body>

</html>